version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  runtime-version: 22
  command: npm run preview -- --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production